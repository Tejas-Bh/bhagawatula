import os

result = [os.path.join(dp, f) for dp, dn, filenames in os.walk("./") for f in filenames]

for i in result:
    str = """"""
    with open(i, encoding="latin-1") as file:
        for l in file:
            str += l
    if "https://" in str:
        print(f"CAUGHT: {i}")
